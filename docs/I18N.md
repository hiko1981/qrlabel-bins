# Locale / i18n (MVP)

- Client locale is derived from `Accept-Language` in requests (see `src/lib/i18n.ts`).
- Locale is stored on `push_subscriptions.locale`.
- Notifications should prefer Danish if locale starts with `da`.

